---
name: etf-session-2026-08-07-golden-dataset-and-sweep
description: "2026-08-07 etf_monitor_system session — regime_ok bug fix, 17 dead tickers fixed, Golden Dataset (frozen OHLCV) built, hyperparameter grid search infrastructure built and validated, wide mm200_distance_max sweep launched."
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-07T16:58:16.819Z
  originSessionId: 1a1d6401-bbcc-42ce-b6a2-6fc42a3f1f19
---

Long single-day session on `etf_monitor_system`. Check [[etf-pending-tasks-2026-08-05]] for older context (mostly superseded now). This memory is the current state as of pausing.

**Already fully done and deployed (do not re-do):**
1. Fixed `UnboundLocalError: regime_ok` in `suggest_level_0()` — was crashing 48/240 ETF (20% of universe) silently, mislabeled as "no data". Fix: moved `regime_ok` computation before its first use (FAST/SLOW L0 paths). Commit `988a5bd`.
2. Fixed/removed 17 dead Yahoo Finance tickers across two rounds (10 then 7 more found during the Golden Dataset backfill): `WSML.DE→WSML.L`, `IWMO.DE→IWMO.L`, `IWQU.DE→IWQU.L`, `MVOL.DE→MVOL.L`, `IGLN.MI→IGLN.L`, `BITC.MI→BITC.SW`, `ETHE.MI→ETHE.SW`, `SLNC.MI→SLNC.SW`, `BTCE.MI→BTCE.DE`, `BDAS.MI→DA21.DE`, `USHYC→USHYC.MI`, `PHAU.MI→PHAU.L`, `3LMS.MI→3LMS.L`; removed `NDXH2.PA` (dup ISIN of `UST.PA`), `3LIS.MI`/`3LUC.MI`/`3MBS.MI` (no live Yahoo listing anywhere). Universe: 240→236 rows in `etf_monitoraggio.xlsx`. Commits `1ea0771`, `a3b57c9`.
3. **Golden Dataset** — `etf_price_history_frozen` Postgres table (migration `002`), populated via `freeze_historical_dataset.py` (229/236 tickers, 2022-02-15→2026-08-07, batch `'2026-08-07'`). Built because `backtest_l1.py` re-fetching live Yahoo data on every run made results non-reproducible (proved empirically: same commit, same params, 2 days apart → 3 trades became 1). `backtest_l1.py` now defaults to reading frozen data (`FrozenDataFetcher`); `--live` for old behavior. Commit `770f183`.
4. **`BASELINE_OFFICIAL_20260807`**: native_7, frozen batch, current code = 1 trade (`WLDC.PA`, entry 2025-08-15, exit TP 2025-10-08, +5.19%/+3.77% net on 10k€ = €376.68). This is THE reference number for future native_7 comparisons on this frozen batch.
5. Closed the "80 vs 3 trade" mystery from 2026-08-06 (see [[etf-l1-smart6-macd-candidate]] and CLAUDE.md) — reproduced the exact 05/08 commit+params on frozen-equivalent conditions, got 1 trade not 3 or 80. Root cause: Yahoo data isn't reproducible run-to-run (adjusted-close revisions), not a code bug.
6. **Performance work for grid search** (all commits `202db3f`, `509b4d3`): `technical_analysis.py::suggest_level()` gained an optional `precomputed` dict param (EMA/SMA/RSI/ADX/MACD/ATR pre-sliced instead of recomputed each day — was O(n²) per ticker). `backtest_l1.py::simulate()` gained `precomputed_full` + `macd_skip_mask` (provably-safe skip: when `require_macd=True`, a day with `macd_ok=False` can never produce an entry, so skip calling `suggest_level()` entirely that day) + hoisted `redirect_stdout` out of the day loop. **Both cross-validated at 0 discrepancies** (`optimize_hyperparameters.py --validate`: 480 checks fast-vs-slow path, plus a second check comparing `simulate()` trade lists with vs without the skip-mask — both PASS). Real measured speedup: ~3.3x (confirmed on `speculativo` cluster, 44s→13s per combo).
7. **VPS hardware constraint discovered**: 1 vCPU only (`nproc`), 3.8GB RAM often <200MB free. Multiprocessing/`ProcessPoolExecutor` gives **zero benefit here** and risks starving the live production Flask+Postgres — the user's external advisor kept re-suggesting it across several messages; corrected each time. The only real lever on this hardware is reducing total work (skip-mask), not parallelizing.
8. **Cluster design for the sweep** (by `sl_initial_pct`, not family names — a prior proposal from the user's external advisor invented family names like `equity_emergenti`/`megatrend` that don't exist in this codebase, caught and corrected):
   - difensivo (2.5-4.0%): bond_governativi, bond_corp_hy_em, settoriali_difensivi, real_estate_reit, private_equity_buffer — 58 tickers
   - core (5.0-6.0%): equity_sviluppati, oro_metalli_preziosi, mercati_emergenti, settoriali_growth, metalli_industriali — 153 tickers
   - speculativo (7.0-12.0%): commodities, leva_single_stock, crypto_digital_assets — 16 tickers
9. **Pilot sweep #1** (mm200_delta ±1pp × adx_delta ±4, 9 combos/cluster, 90.5 min, ran on OLD unoptimized engine): no combo reached N≥30 anywhere. core: 6-22 trades. speculativo: 0 trades on all 9. difensivo: 2-3 trades, 0% win rate in-sample on every single combo.
10. **Resolved the ~26-vs-151-trade discrepancy** (pilot total vs the 05/08 smart_6_macd full-universe number) via targeted diff-debug on `CHIP.MI` (had 4 trades in the 05/08 run): original `backtest_l1.py` unmodified AND the optimized sweep engine both give 0 trades under current code (proving the sweep engine has zero bug) — but disabling `mm200_distance_max` (set to 999) restores exactly the original 4 trades. Cause: `mm200_distance_max` was added 2026-08-06 (commit `2deb026`), **after** the 151-trade measurement — same "code evolved between runs" story as the native_7 3→1 case, just much more severe here since the family baseline (3.0% for equity_sviluppati) apparently kills nearly everything. This is fully documented in CLAUDE.md.

**Wide sweep completed (2026-08-07, same session)**: 45 combos, 44.9 min. Results (all in CLAUDE.md "Grid Search smart_6_macd — sweep ampio 2026-08-07"):
- **core** (153 tickers): only cluster with usable signal. 5 combos clear N≥30 in-sample. **Candidate Entry Zone: `mm200_distance_max=7.0%, adx_entry` baseline-4`** — IN N=33 PF=1.18 WR=57.6%, OUT N=25 PF=1.65 WR=68.0% (out-of-sample *improves* on in-sample, the opposite of overfitting — the strongest signal in the whole sweep). Runner-up: `mm200=OFF, adxΔ=+4` (IN N=39 PF=1.25, OUT N=57 PF=1.23 — very stable, more N but weaker PF). NOT promoted to production — still a backtest candidate, live system stays locked until 2026-09-06.
- **difensivo** (58 tickers): doesn't work regardless of mm200 — max N=9 even with mm200 fully OFF, in-sample win rate 0-33% almost everywhere. Bottleneck isn't mm200_distance_max here, it's signal scarcity (bonds/defensive/REIT/PE rarely produce a positive-and-accelerating MACD histogram). `smart_6_macd` unsuitable for this cluster, full stop.
- **speculativo** (16 tickers): 0 trades at mm200 3-9%, only 1 trade with mm200 fully OFF. Bottleneck not yet isolated (likely `macd_ok` or RSI/ADX window too tight for crypto/leva/commodities) — confirmed finding from pilot #1, held up in the wide sweep too.

**Phase 2 + TP micro-sweep — DONE, same session.** Full results in CLAUDE.md "Fase 2 + micro-sweep TP (2026-08-07) — CANDIDATE_MODEL_B_20260807":
- Phase 2 (36 combos, 69 min, core cluster only, entry zone fixed): swept `sl_buffer_wide` mult [1.0-1.8x] × `target_max_pct` [10-15%] × `target_floor_pct` [3-5%, turned out to have ZERO measurable effect — never the binding constraint, wasted grid dimension]. **Key finding: widening SL makes everything worse** — PF drops 1.45→0.96 and MaxDD explodes 32.5%→66% as sl_mult goes 1.0x→1.8x. SL at baseline (1.0x, unchanged) is the only sustainable choice.
- TP micro-sweep (5 combos, 9.9 min, SL fixed at baseline): extended target_max_pct to 15/18/20/22/25% to check if the Phase-2 upward trend (10%→1.27, 12%→1.32, 15%→1.45) continued. It didn't: 18%→1.30 (dips), 20%→1.33, 22%→1.36, 25%→1.36 — flat/noisy plateau, not a higher peak. N and win rate identical (31, 54.8%) across the whole range — TP only changes *where* trades exit, not which trades happen. **15% remains the best point found**, correctly didn't chase the noise beyond it.

**CANDIDATE_MODEL_B_20260807 — final, documented, NOT deployed**: core cluster only (equity_sviluppati, oro_metalli_preziosi, mercati_emergenti, settoriali_growth, metalli_industriali), `smart_6_macd` variant (min_buy_count=6, macd_ok always required): `mm200_distance_max=7.0%` (absolute), `adx_entry`=baseline−4, `sl_buffer_wide` unchanged, `l1_stop_gain_dynamic.target_max_pct=15%`. Certified metrics: IN (2023-08-05→2025-08-05) N=31 PF=1.45 WR=54.8% MaxDD=32.5%; OUT (2025-08-05→2026-08-05) N=18 PF=1.62 WR=55.6% MaxDD=19.1%.

**Explicitly NOT promoted to production** — three reasons on record: (1) the 30-day parameter lockdown until 2026-09-06 was already decided earlier this session, (2) `smart_6_macd` has never been live (`use_smart_6_7_macd: false` everywhere) — deploying this candidate would also mean deciding to switch the entry gate itself, a separate decision never made explicitly, (3) N=31/18 is still a modest sample, barely at/under the N≥30 bar.

**Shadow Monitor — IMPLEMENTED AND LIVE, same session (commits `0be6673`, `41d0c11`).** User picked: DB-only logging (no daily email), scoped to only the 5 `core` families (via AskUserQuestion). Built:
- `migrations/003_add_shadow_positions.sql` — new table `etf_shadow_positions` (model_name, ticker, isin, famiglia, entry/exit date+price, exit_reason, status, gross_pct_gain). Applied to prod Postgres.
- `database.py` — `open_shadow_position()`, `close_shadow_position()`, `get_open_shadow_position()`, `get_shadow_positions()`.
- `shadow_monitor.py` (new file) — `run_shadow_monitor(db, results, add_log)`: for each core-family ETF in the already-computed `results` list, checks for an open shadow position (close it if Candidate B's SL/TP trigger, using the real `calculate_sl_suggerito_l1`/`calculate_stop_gain_dynamic` functions with Candidate B's params) or evaluates a new entry (fetches live OHLC via `db.get_ohlc_by_isin`, calls the real `suggest_level()` with Candidate B's parameter overrides — `make_candidate_b_analyzer()` mirrors the exact override pattern from `optimize_hyperparameters.py`). Zero logic duplication — same production functions, only params differ.
- `monitor.py::run()` — new STEP 8, calls `run_shadow_monitor()` wrapped in try/except so it can never block/crash the real cycle (same discipline as every other step in that function).
- **Verified end-to-end on live production**: ran cleanly across all ~155 core-family tickers with no errors (both via the real triggered monitor cycle and a manual standalone invocation with verbose logging). 0 shadow entries on day 1 — expected and correct, consistent with the backtest's low base rate (~31 trades/2yr across the whole cluster) and confirmed the regime-BULL gate correctly blocks entries even when buy_count alone would qualify.
- **Extraction query for 2026-09-06** (end of lockdown): `SELECT ticker, entry_date, exit_date, exit_reason, gross_pct_gain FROM etf_shadow_positions WHERE model_name = 'candidate_model_b_20260807' ORDER BY entry_date;`

Session is now fully complete — all work committed and pushed (last commit `41d0c11`), CLAUDE.md fully documents the day end-to-end, live system now silently tracking Candidate Model B in parallel with zero risk to production decisions.

**Style/collaboration note**: the user is relaying analysis from an external AI advisor throughout this session (pasted, heavily formatted text with emoji headers) and asking "cosa ti sembra? procediamo?" — the user wants genuine critical pushback, not agreement. Real catches that mattered: multiprocessing on 1-vCPU hardware (repeatedly re-suggested, always wrong here), invented family names not in the codebase, treating a placeholder example number ("78 trade, ipotizziamo") as if real. Keep verifying claims against the actual codebase/hardware before agreeing, every time — this pattern held for the whole session, not a one-off.
